<div right=20px>
	<h1><%= @challenge.title %></h1>
	<p><%= @challenge.description %></p>

	<%= form_for @challenge do |c| %>
		<div>
			<%= c.text_area :body, size:"60x20" %>
		</div>

		<div>
			<%= c.submit "Submit!" %>
		</div>
	<% end %>
</div>